{
  "name": "@remocoder/shared",
  "version": "0.0.1",
  "private": true,
"main": "./src/index.ts",
  "scripts": {
    "lint": "oxlint src",
    "lint:fix": "oxlint src --fix",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
