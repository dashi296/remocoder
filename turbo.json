{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "lint": {
      "cache": true,
      "inputs": ["src/**", "../../.oxlintrc.json"]
    },
    "lint:fix": {
      "cache": false
    },
    "test": {
      "cache": true,
      "inputs": [
        "src/**",
        "vitest.config.*",
        "jest.config.*",
        "tsconfig.json",
        "package.json"
      ],
      "outputs": [
        "coverage/**"
      ]
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    }
  }
}
